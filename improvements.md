# Improvements
These are improvements I would like to see.

- `internal/slack/formatter.go` and `internal/slack/slack_parser.go` are heavily duplicated.
- `internal/slack/slack_updater.go` has `oldFormatter` which looks like a partially migrated adapter.