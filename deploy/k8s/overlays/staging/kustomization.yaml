apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: marvin-staging

patchesStrategicMerge:
  - slacker-patch.yaml

images:
  - name: slacker
    newTag: staging